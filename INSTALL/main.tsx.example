// =============================================================
// main.tsx — add ONE import for the global theme.css
// =============================================================

import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './styles/index.css';
import './styles/theme.css';  // ← ADD THIS LINE (the new global theme rules)

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
